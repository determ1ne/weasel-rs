[package]
name = "weasel-broker"
version = "0.1.3"
edition = "2024"
license.workspace = true

[dependencies]
serde_json = "1"
weasel-common = { path = "../common" }
tokio = { version = "1", features = ["io-util", "net", "rt", "time", "process"] }
windows-core = "0.100.0"

windows-strings = "0.100.0"
windows-collections = "0.100.0"

[build-dependencies]
embed-resource = "3"
windows-bindgen = "0.100.0"
